import requests import streamlit as st api_key = st.secrets["GEMINI_API_KEY"] url = "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent" headers = { "Content-Type": "application/json", "x-goog-api-key": api_key } data = { "contents": [ { "parts": [ { "text": "Responde únicamente: GEMINI FUNCIONA" } ] } ] } respuesta = requests.post( url, headers=headers, json=data, timeout=60 ) st.write("HTTP:", respuesta.status_code) st.code(respuesta.text)
